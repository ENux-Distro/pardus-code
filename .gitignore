# The PARDUS-branded opencode binary is large (~139 MB) and is built from the
# fork at install time (see packaging/build-opencode.sh), never committed.
vendor/

# Build output
build/
dist/

# Editor / OS noise
*.swp
*~
.DS_Store
