# The PARDUS-branded opencode binary is large (~139 MB) and is distributed via
# GitHub Releases, not git. Build it and drop it here for local installs / deb.
vendor/

# Build output
*.deb
build/
dist/

# Editor / OS noise
*.swp
*~
.DS_Store
